<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             x:Class="Grocery.App.Views.GroceryListItemsView"
             x:DataType="vm:GroceryListItemsViewModel"
             Title="GroceryListItemsView">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Wijzig kleur" Command="{Binding ChangeColorCommand}" 
                     IconImageSource="{FontImage Glyph='&#xf30c;', Color=Black, Size=22}" />
        <ToolbarItem Text="Deel boodschappenlijst" Command="{Binding ShareGroceryListCommand}" 
                     IconImageSource="{FontImage Glyph='&#xf30c;', Color=Black, Size=22}" />
    </ContentPage.ToolbarItems>

    <Shell.TitleView>
        <Grid>
            <Label Text="{Binding GroceryList.Name}" FontSize="Large" 
                   HorizontalOptions="Center" VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>
    <Grid RowDefinitions="2*,3*">

        <!-- Boodschappenlijst -->
        <Border Grid.Row="0" Stroke="#C49B33" StrokeThickness="4" Padding="10" Margin="10">
            <CollectionView ItemsSource="{Binding MyGroceryListItems}" Margin="20"
                            EmptyView="De boodschappenlijst is nog leeg. Voeg eerst boodschappen toe!">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:GroceryListItem">
                        <Grid ColumnDefinitions="4*,*,20,20">
                            <Label Grid.Column="0" Text="{Binding Product.Name}" VerticalOptions="Center"/>
                            <Label Grid.Column="1" Text="{Binding Amount}" VerticalOptions="Center" 
                                   HorizontalOptions="End" Margin="0,0,10,0"/>
                            <Image Grid.Column="2" Source="plus.png" WidthRequest="15" HeightRequest="15"/>
                            <Image Grid.Column="3" Source="min.png" WidthRequest="15" HeightRequest="15"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Border>

        <!-- Zoekveld + Productenlijst -->
        <Border Grid.Row="1" Stroke="#C49B33" StrokeThickness="4" Padding="10" Margin="10">
            <VerticalStackLayout Spacing="10">
                <!-- Zoekveld -->
                <SearchBar Placeholder="Zoek product..."
                           SearchCommand="{Binding SearchCommand}"
                           SearchCommandParameter="{Binding Text, Source={RelativeSource Self}}" />

                <!-- Beschikbare producten -->
                <CollectionView ItemsSource="{Binding AvailableProducts}" SelectionMode="Single"
                                SelectionChangedCommand="{Binding AddProductCommand}"
                                SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="m:Product">
                            <Grid ColumnDefinitions="3*,*">
                                <Label Grid.Column="0" Text="{Binding Name}" VerticalOptions="Center"/>
                                <Label Grid.Column="1" Text="{Binding Stock}" VerticalOptions="Center" 
                                       HorizontalOptions="End" Margin="0,0,10,0"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.EmptyView>
                        <ContentView>
                            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                                <Label Text="Er zijn geen producten meer om toe te voegen" 
                                       FontAttributes="Bold" HorizontalTextAlignment="Center"/>
                            </VerticalStackLayout>
                        </ContentView>
                    </CollectionView.EmptyView>
                </CollectionView>
            </VerticalStackLayout>
        </Border>

        <!-- Bericht -->
        <Label Grid.Row="0" HorizontalOptions="Center" 
               Text="{Binding MyMessage}" TextColor="Red" Margin="30,0,0,10"/>
    </Grid>
</ContentPage>
